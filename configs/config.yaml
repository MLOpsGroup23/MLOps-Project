data:
  raw_dir: data/raw
  processed_dir: data/processed
  batch_size: 1000
  train:
    transformations:
      rotation:
        use: True
        degrees: 5
      scale:
        use: False
  test:
    transformations:
      rotation:
        use: False
        degrees: 5